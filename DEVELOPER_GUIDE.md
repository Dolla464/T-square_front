# 🛠️ دليل المطورين لمنصة T-Square (Developer Guide)

مرحباً بك في دليل التطوير الخاص بـ **منصة T-Square**. تم إعداد هذا الدليل لمساعدة المهندسين والمطورين على فهم بنية المشروع، ومعايير التصميم، وكتابة الكود بشكل متناسق وآمن، بالإضافة إلى سير العمل اليومي الخاص بـ Git ونظام الترجمة والـ Responsive.

---

## 📁 1. هيكل المشروع وبنية المجلدات (Project Structure)

يعتمد المشروع على **React + Vite** مع مكتبة **Bootstrap** للتنسيقات. ينقسم مجلد الكود المصدري `src` إلى ما يلي:

* **`src/pages/`**: يحتوي على الصفحات العامة للمنصة (مثل الصفحة الرئيسية `Home` والكورسات `CoursesPage` والحلول `Solutions` وفريق العمل `Team` واتصل بنا `Contact` والتسجيل `Login`/`Signup`).
* **`src/components/`**: المكونات (Components) المشتركة والمعاد استخدامها مقسمة حسب النطاق (مثل `layout` للنافبار والفوتر، و `shared` للمكونات العامة مثل نوافذ التأكيد أو حماية المسارات).
* **`src/modules/`**: النطاقات المعزولة أو لوحات التحكم المعقدة:
  * `student-dashboard/`: تحتوي على لوحة تحكم الطالب وتفاصيل كورساته والاختبارات والشهادات.
  * `admin-dashboard/`: تحتوي على لوحة تحكم المسؤول (الأدمن) لإدارة المستخدمين، الكورسات، الشهادات، والمجموعات.
  * `shared-dashboard/`: المكونات المشتركة بين الداشبوردات مثل صفحة الإشعارات.
* **`src/contexts/`**: سياق الحالة المشترك مثل ملف `AuthContext.jsx` لإدارة حالة تسجيل الدخول وصلاحيات المستخدمين.
* **`src/locales/`**: ملفات الترجمة والتدويل (i18n) للغتين العربية (`ar`) والإنجليزية (`en`).
* **`src/assets/`**: الصور، الأيقونات، والخطوط.

---

## 🧭 2. نظام التوجيه وإدارة الصلاحيات (Routing & Security)

يتم تنظيم جميع مسارات التطبيق داخل ملف `src/App.jsx`. ينقسم التوجيه إلى ثلاثة مستويات رئيسية:

1. **المسارات العامة (Public Routes)**: متاحة لجميع الزوار (مثل `/`, `/courses`, `/solutions`, `/team`).
2. **مسارات الطالب المحمية (Student Dashboard)**: مسارات تبدأ بـ `/student/*` ومحمية بواسطة المكون `ProtectedRoute` بحيث يُسمح فقط للمستخدمين ذوي الصلاحية `student` بالوصول إليها.
3. **مسارات الأدمن المحمية (Admin Dashboard)**: مسارات تبدأ بـ `/admin/*` ومحمية بـ `ProtectedRoute` وتتطلب صلاحية `admin`.

> [!IMPORTANT]
> عند إنشاء مسار جديد يتطلب حماية، تأكد من إضافته داخل كتلة الـ `Route` الخاصة بالصلاحية المناسبة في `App.jsx` لمنع الوصول غير المصرح به.

---

## 🌍 3. نظام الترجمة والتدويل (i18n & RTL/LTR)

تدعم المنصة اللغتين **العربية (RTL)** و **الإنجليزية (LTR)** بالكامل باستخدام مكتبة `react-i18next`.

### كيفية إضافة مفاتيح ترجمة جديدة:
* يتم وضع ملفات الترجمة في المجلد `src/locales/ar/` و `src/locales/en/`.
* كل قسم من الموقع له ملف JSON خاص به (مثل `adminDashboard.json` لإدارة لوحة الأدمن، و `auth.json` لعمليات الدخول).
* لاستخدام الترجمة في المكونات:
  ```jsx
  import { useTranslation } from 'react-i18next';
  
  function MyComponent() {
    const { t } = useTranslation('adminDashboard'); // تحديد اسم الملف بدون الامتداد
    return <h1>{t('key_name')}</h1>;
  }
  ```

### مزامنة اتجاه الصفحة (dir):
يتم تحديث اتجاه الموقع (`dir="rtl"` أو `dir="ltr"`) تلقائياً في ملف `App.jsx` عند تغيير اللغة وحفظ الخيار في `localStorage` لضمان تجربة مستخدم سلسة.

---

## 🧱 4. دليل تطوير صفحات لوحة التحكم للأدمن (Admin CRUD Template)

لتسريع وتوحيد العمل في لوحة التحكم، تم توفير نموذج إعادة استخدام جاهز لعمليات CRUD (عرض، إضافة، تعديل، حذف) مثل صفحة `AdminStudents.jsx`. 

### خطوات إنشاء صفحة جديدة للأدمن (مثلاً لإدارة المحاضرين `AdminInstructors.jsx`):
1. **نسخ الملف**: قم بنسخ هيكل صفحة الطلاب وتغيير اسم الملف.
2. **تحديث البيانات الوهمية (Mock Data)**: قم بتغيير الداتا الأساسية والـ State لتناسب الحقول الجديدة للمحاضرين (الاسم، البريد، التخصص، إلخ).
3. **تحديث الـ State وفلاتر البحث**: قم بتعديل منطق التصفية والبحث ليتطابق مع الحقول الجديدة.
4. **تعديل الجدول**: غيّر أعمدة الجدول وربطها بمفاتيح الترجمة المناسبة.
5. **تحديث النموذج (Form)**: تأكد من تعديل حقول الإدخال (Inputs) لتطابق الحقول الجديدة، والتأكد من تحديث حالات الإنشاء والتعديل.
6. **إضافة ملف الترجمة**: أضف الترجمات المقابلة في ملفات الترجمة لضمان عدم وجود نصوص ثابتة (Hardcoded Strings).

> [!WARNING]
> لا تقم بتعديل كلاسات الـ CSS العامة مثل `ac-table`, `ac-form`, `ac-btn` لأنها مشتركة وتضمن اتساق التصميم عبر جميع الصفحات.

---

## 📱 5. إرشادات التصميم المتجاوب (Responsive Design)

تعتمد المنصة على تصميم متجاوب تماماً، خصوصاً للوحات التحكم المعقدة، مع الحفاظ على التصميم الأصلي لشاشات الحاسوب:

* **شريط الفلاتر والبحث (`ac-filters-bar`)**: يتحول إلى الوضع الرأسي (`flex-direction: column`) على الموبايل لعدم تداخل الحقول.
* **توزيع الفلاتر (CSS Grid)**: يتم ترتيب القوائم المنسدلة على هيئة عمودين متساويين في شاشات الموبايل. وإذا كان العدد فردياً (مثل 3 فلاتر)، يمتد الفلتر الثالث ليأخذ عرض الصف كاملاً تلقائياً.
* **تجاوب جداول البيانات**: يتم تفعيل خاصية التمرير الأفقي للجداول بمرونة (`.table-responsive`) مع حظر كسر النصوص وتكدس الكلمات باستخدام `white-space: nowrap`.
* **إخفاء النصوص وتوفير المساحة**: في شاشات الهاتف الضيقة، يتم إخفاء النصوص التوضيحية لبعض الأزرار (مثل "إضافة طالب" أو "سلة المحذوفات") والاكتفاء بالأيقونة المرفقة لتقليص الحجم الزائد وتجنب تشوه الواجهة.
* **بطاقات الإحصائيات (Stats Cards)**: تأخذ عرضاً كاملاً بنسبة `col-12` على الهواتف بدلاً من التكدس، مع الحفاظ على `col-md-3` للحواسب.

---

## 🔄 6. سير العمل على Git والمزامنة اليومية (Git Workflow)

للحفاظ على سلامة الكود وتجنب حدوث تعارضات (Conflicts) معقدة، يجب على كل مطور الالتزام بالخطوات التالية:

### 🌅 بداية اليوم (التحديث والانطلاق)
قبل كتابة أي كود جديد، احصل على أحدث نسخة وتخلص من الفروع المحذوفة:
```bash
# تنظيف الفروع المحذوفة محلياً
git fetch -p

# الانتقال لبرانش التطوير الأساسي وسحب أحدث التحديثات
git checkout develop
git pull origin develop

# إنشاء فرعك الخاص للمهمة (باسم واضح)
git checkout -b "MohamedSalama/feature/my-task"
```

### 💻 منتصف اليوم (الحفظ المرحلي)
القاعدة الأساسية هي **"احفظ مبكراً، واحفظ كثيراً"**. لا تنتظر لنهاية اليوم لعمل التزام (Commit) واحد ضخم.
```bash
git add .
git commit -m "أضف هنا وصفاً دقيقاً ومحدداً للميزة أو التعديل المنجز"
```

### 🌌 نهاية اليوم أو اكتمال المهمة (المزامنة والرفع)
للتأكد من خلو كودك من التعارضات مع عمل زملائك:
1. احفظ عملك الحالي في فرعك بـ Commit.
2. انتقل لفرع التطوير الأساسي واسحب التحديثات الجديدة:
   ```bash
   git checkout develop
   git pull origin develop
   ```
3. عُد إلى فرعك وادمج التحديثات داخله:
   ```bash
   git checkout MohamedSalama/feature/my-task
   git merge develop
   ```
4. قم بحل التعارضات (Conflicts) إن وجدت واختبر الكود محلياً.
5. ارفع فرعك إلى GitHub بأمان:
   ```bash
   git push -u origin MohamedSalama/feature/my-task
   ```

### 🚑 استراتيجية الإنقاذ السريع والتراجع (Emergency Cheatsheet)
* للتراجع عن جميع التعديلات غير المحفوظة في ملفاتك:
  ```bash
  git restore .
  ```
* للتراجع عن آخر التزام (Commit) مع الاحتفاظ بالكود وتعديلاته:
  ```bash
  git reset --soft HEAD~1
  ```
* للتراجع عن آخر التزام ومسح الكود تماماً والرجوع للنقطة السابقة:
  ```bash
  git reset --hard HEAD~1
  ```
* لتصفير المستودع المحلي بالكامل والعودة لأحدث نسخة مطابقة لـ GitHub:
  ```bash
  git fetch origin
  git reset --hard origin/develop
  ```

---

## 🚀 7. تشغيل المشروع محلياً (Local Development Commands)

### تشغيل الواجهة الأمامية (Frontend)
1. تثبيت الحزم (أول مرة فقط):
   ```bash
   npm install
   ```
2. تشغيل سيرفر التطوير المحلي:
   ```bash
   npm run dev
   ```
3. بناء نسخة الإنتاج:
   ```bash
   npm run build
   ```

### تشغيل الواجهة الخلفية (Backend - Laravel) إن وجدت محلياً
1. تثبيت حزم الملحن:
   ```bash
   composer install
   ```
2. نسخ ملف البيئة وتوليد المفتاح:
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```
3. تشغيل الهجرة وتغذية قاعدة البيانات:
   ```bash
   php artisan migrate:fresh --seed
   ```
4. تشغيل سيرفر الـ Backend:
   ```bash
   php artisan serve
   ```
5. مسح الكاش عند حدوث مشاكل في التهيئة:
   ```bash
   php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear
   ```
